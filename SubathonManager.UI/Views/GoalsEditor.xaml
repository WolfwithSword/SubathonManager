<UserControl x:Class="SubathonManager.UI.Views.GoalsEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Padding="8" BorderThickness="1" BorderBrush="#333" CornerRadius="8">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions>
                    <TextBlock Text="Goals" FontWeight="Bold" FontSize="18" Margin="4"/>
                    <ui:Button Grid.Column="1" Width="60" Height="30" Padding="2" Margin="38 0 0 0 "
                               ToolTip="New Goal" Click="AddGoal_Click">
                        <ui:SymbolIcon HorizontalAlignment="Center" Symbol="Add24" Width="20" Height="20" Margin="0,0,0,0"/>
                    </ui:Button>
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="8 12 8 2">
                    <TextBlock Text="Goal" Width="522"/>
                    <TextBlock Text="Points" Width="60" Margin="8 0 0 0 "/>
                </StackPanel>
                <ScrollViewer x:Name="GoalsEditorScroller" MaxHeight="600" Height="600" VerticalScrollBarVisibility="Visible" Margin="0 0 0 0 ">
                    <StackPanel Margin="0 2 0 0 " x:Name="GoalsStack" Orientation="Vertical"/>
                </ScrollViewer>
            </StackPanel>
        </Border>
        
        <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Top">
            
            <TextBlock Text="Goals Editor" FontSize="20" FontWeight="Bold"/>

            <StackPanel Orientation="Horizontal" Margin="0 6 0 0 ">
                <TextBlock Text="Name:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 6 8 6"/>
                <TextBox x:Name="GoalSetNameBox" Width="260" Height="34" Margin="0 0 12 0"/>
                <ComboBox Text="Type" ToolTip="Points will be based off points. Money is based on total donations."
                          x:Name="GoalSetType" Width="120" Height="34"/>
                
            </StackPanel>
            <Grid Margin="0 16 0 8 ">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="MoneyValue" HorizontalAlignment="Left" 
                           Text="" VerticalAlignment="Center" FontSize="24" Margin="16 2 8 6"/>
                <TextBlock Grid.Column="1" x:Name="PointsValue" HorizontalAlignment="Right" 
                           Text="" VerticalAlignment="Center" FontSize="24" Margin="0 2 16 6"/>
            </Grid>
            <StackPanel Orientation="Horizontal">
                <ui:Button Content="Create New List" Click="CreateNewGoalSet_Click" Width="160" Height="30"/>
            </StackPanel>

            <Separator Margin="0,10,0,10"/>

            <ui:Button x:Name="SaveGoalsBtn" Content="Save Changes" Click="SaveGoals_Click" Width="140" Height="50" Margin="0 4 0 4"/>

            <TextBlock x:Name="StatusText" Foreground="Gray" FontSize="12" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
