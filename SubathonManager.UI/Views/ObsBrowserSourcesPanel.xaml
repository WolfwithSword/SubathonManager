<UserControl x:Class="SubathonManager.UI.Views.ObsBrowserSourcesPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="2 10 2 6">
            <ui:Button DockPanel.Dock="Right" Width="32" Height="32" Padding="2"
                       ToolTip="Refresh browser sources from OBS"
                       Click="Refresh_Click">
                <ui:SymbolIcon Symbol="ArrowClockwise24" Width="16" Height="16"/>
            </ui:Button>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="OBS Overlay Sources" FontWeight="Bold" FontSize="16"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="PanelStatusText" FontSize="11" Margin="10 2 0 0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0 0 2 2">
            <StackPanel x:Name="CardsStack" Orientation="Vertical"/>
        </ScrollViewer>
    </Grid>
</UserControl>
