<external:DevTunnelSettingsControl x:Class="SubathonManager.UI.Views.SettingsViews.External.KoFi.KoFiWebhookSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:views="clr-namespace:SubathonManager.UI.Views"
             xmlns:external="clr-namespace:SubathonManager.UI.Views.SettingsViews.External">

    <StackPanel>

        <!-- Integration status row -->
        <StackPanel Orientation="Horizontal" Margin="0,2,0,8">
            <ui:TextBlock Text="Status:" Width="80" VerticalAlignment="Center" FontSize="12"/>
            <ui:TextBlock x:Name="KoFiWebhookStatusText" Text="Disconnected" VerticalAlignment="Center" FontSize="12"/>
        </StackPanel>

        <!-- DevTunnels prerequisite status -->
        <Border x:Name="TunnelStatusBorder" BorderThickness="1" CornerRadius="4" Padding="8,6"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                Background="{DynamicResource ControlFillColorDefaultBrush}"
                Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="DevTunnels:" Width="110" VerticalAlignment="Center" FontSize="12"/>
                    <ui:TextBlock x:Name="TunnelPrereqStatusText" Text="Not running"
                               VerticalAlignment="Center" FontSize="12" Margin="0,0,12,0"/>
                    <ui:Button Content="Go to setup" Click="GoToDevTunnels_Click" x:Name="TunnelPrereqHint" FontSize="11" 
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <!-- <TextBlock Text="(Go to DevTunnels tab to set up)" -->
                    <!--            x:Name="TunnelPrereqHint" -->
                    <!--            VerticalAlignment="Center" FontSize="11" -->
                    <!--            Foreground="{DynamicResource TextFillColorSecondaryBrush}"/> -->
                </StackPanel>
                <StackPanel x:Name="WebhookUrlRow" Orientation="Horizontal" Margin="0,8,0,2" Visibility="Collapsed"
                            MinHeight="36">
                    <TextBlock Text="Webhook URL:" Width="110" VerticalAlignment="Center" FontSize="12"/>
                    <ui:PasswordBox x:Name="WebhookUrlBox" Width="420" Height="34" IsReadOnly="True"
                                views:SettingsProperties.ExcludeFromUnsaved="True" ClearButtonEnabled="False"
                                FontSize="11" VerticalAlignment="Center"/>
                    <ui:Button Content="Copy"
                               Click="CopyWebhookUrl_Click"
                               Width="56" Height="34" Padding="4,2"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               Margin="6,0,2,0"/>
                    
                    <ui:Button Content="Setup KoFi" Margin="4 0" Width="112" Height="34" Click="OpenKoFiTokenLink_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Configuration -->
        <StackPanel Margin="0,2">
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Verification Token:" Width="160" VerticalAlignment="Center"
                           ToolTip="The verification token from your Ko-fi webhook settings page. Found at Ko-fi → My Account → API → Webhooks."/>
                <ui:PasswordBox x:Name="KoFiWebhookTokenBox" Width="360" Height="34"
                            PlaceholderText="paste token here"
                            ToolTip="Copy this from Ko-fi → My Account → API → Webhooks"/>
                <ui:Button Content="Get Token" Margin="4 0" Width="116" Height="34" Click="OpenKoFiTokenLink_Click"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,4">
                <TextBlock Text="Forward URL(s):" Width="160" VerticalAlignment="Center"
                           ToolTip="A list of URLs to forward the raw Ko-Fi webhook to. Leave blank to disable."/>
                <ui:TextBox x:Name="KoFiWebhookForwardUrlsBox" Width="360" Height="34" IsReadOnly="True" ClearButtonEnabled="False"
                            PlaceholderText="No URLs configured" MouseDoubleClick="EditForwardUrls_Click"
                            PreviewMouseLeftButtonUp="EditForwardUrls_Click"
                            ToolTip="Click to edit, one URL per line"/>
                <!-- <ui:Button Content="Edit" Width="116" Height="34" Margin="4,0,0,0" -->
                <!--            Padding="4,2" Click="EditForwardUrls_Click" -->
                <!--            Foreground="{DynamicResource TextFillColorPrimaryBrush}"/> -->

                <Popup x:Name="ForwardUrlsPopup" StaysOpen="True" Placement="Bottom"
                       PlacementTarget="{Binding ElementName=KoFiWebhookForwardUrlsBox}"
                       AllowsTransparency="True">
                    <Border Background="{DynamicResource ApplicationBackgroundBrush}"
                            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                            BorderThickness="1" CornerRadius="6" Padding="10"
                            MinWidth="450">
                        <StackPanel>
                            <TextBlock Text="One URL per line:" FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       Margin="0,0,0,6"/>
                            <ui:TextBox x:Name="ForwardUrlsMultiBox"
                                     AcceptsReturn="True" TextWrapping="NoWrap"
                                     MinHeight="80" MaxHeight="240" Width="500"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                     FontSize="12" Padding="4"
                                     BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                                     BorderThickness="1"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                <ui:Button Content="Cancel" Width="70" Height="28" Padding="4,2"
                                           Margin="0,0,6,0" Click="ForwardUrlsCancel_Click"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <ui:Button Content="Apply" Width="70" Height="28" Padding="4,2"
                                           Click="ForwardUrlsApply_Click"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Popup>
            </StackPanel>
        </StackPanel>
    </StackPanel>

</external:DevTunnelSettingsControl>
