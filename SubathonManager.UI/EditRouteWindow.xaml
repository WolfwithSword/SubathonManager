<ui:FluentWindow x:Class="SubathonManager.UI.EditRouteWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:SubathonManager.UI"
                 xmlns:validation="clr-namespace:SubathonManager.UI.Validation"
                 xmlns:conv="clr-namespace:SubathonManager.UI.Converters"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:views="clr-namespace:SubathonManager.UI.Views"
                 xmlns:uiUtils="clr-namespace:SubathonManager.UI.UiUtils"
                 mc:Ignorable="d"
                 Background="{DynamicResource AppGeneralBackground}"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterOwner" MinHeight="500" MinWidth="940"
                 Title="Overlay Editor" Height="900" Width="1740">
    <ui:FluentWindow.Resources>
        <conv:CssColorStringToColorConverter x:Key="CssColorConverter"/>
        <conv:CssVariableTypeOptionsConverter x:Key="CssTypeOptionsConverter"/>
        <conv:CssSizeValueConverter x:Key="CssSizeValueConverter"/>
        <conv:CssSizeUnitConverter x:Key="CssSizeUnitConverter"/>
        <conv:NullOrEmptyToNullConverter x:Key="NullOrEmptyToNull"/>
        <conv:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <Style x:Key="OpaqueSecondaryButton" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
            <Setter Property="Appearance" Value="Secondary"/>
            <Setter Property="Background" Value="{DynamicResource OpaqueSecondaryBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource OpaqueSecondaryBackgroundBorder}"/>
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ui:Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource OpaqueSecondaryBackgroundHover}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource OpaqueSecondaryBackgroundHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource OpaqueSecondaryBackgroundPressed}"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource OpaqueSecondaryBackgroundPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ui:FluentWindow.Resources>
    <Grid
        Background="{DynamicResource AppGeneralBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="370"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="370"/>
        </Grid.ColumnDefinitions>
        
        
        <ui:TitleBar Title="Overlay Editor" Grid.ColumnSpan="3"
                     ShowMinimize="True"
                     Background="{DynamicResource AppTitleBackground}"
                     Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                     VerticalContentAlignment="Top"
                     ShowMaximize="True"
                     ShowClose="True"
                     FontSize="14"
                     Padding="12 8 12 8"
                     DockPanel.Dock="Top">
            <ui:TitleBar.Icon>
                <ui:ImageIcon VerticalAlignment="Top"
                              RenderOptions.BitmapScalingMode="HighQuality"
                              SnapsToDevicePixels="True"
                              UseLayoutRounding="True">
                    <ui:ImageIcon.Source>
                        <BitmapImage UriSource="pack://application:,,,/icon.png" />
                    </ui:ImageIcon.Source>
                </ui:ImageIcon>
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        
        <!-- LEFT - widget list and route settings -->
        <Grid Grid.Column="0" Margin="12 44 12 12" >
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Vertical" Background="{DynamicResource UiBrushControlBackground}">
                <TextBlock Text="Overlay" FontWeight="SemiBold" Margin="0,12,0,8"/>
                <TextBlock Text="Name" FontSize="12" />
                <ui:TextBox x:Name="RouteNameBox" Margin="0,4,0,8" PlaceholderText="Overlay Name..."/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Margin="0 2 2 2">
                        <TextBlock Text="Width" FontSize="12"/>
                        <ui:TextBox x:Name="RouteWidthBox" Width="120" PreviewTextInput="NumberOnly_PreviewTextInput" PlaceholderText="1920"/>
                    </StackPanel>
                    <StackPanel Margin="2 2 0 2">
                        <TextBlock Text="Height" FontSize="12"/>
                        <ui:TextBox x:Name="RouteHeightBox" Width="120" PreviewTextInput="NumberOnly_PreviewTextInput" PlaceholderText="1080"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <ui:Button Padding="2"
                               ToolTip="Copy URL to clipboard"
                               Width="32" Height="32"
                               Margin="4,0,4,0"
                               Click="CopyOverlayUrl_Click"><ui:SymbolIcon Symbol="Link24" />
                    </ui:Button>

                    <ui:Button Padding="2" ToolTip="Open in browser"
                               Width="32" Height="32"
                               Margin="4,0,4, 0"
                               Click="OpenOverlayInBrowser_Click"><ui:SymbolIcon Symbol="Open24"/>
                    </ui:Button>

                    <ui:Button Padding="2" ToolTip="Export"
                               Width="32" Height="32"
                               Margin="4,0,154,0"
                               Click="ExportRoute_Click"><ui:SymbolIcon Symbol="ArrowExportUp24"/>
                    </ui:Button>
                    <ui:Button x:Name="SaveRouteButton" Content="Save" Width="80" Click="SaveRouteButton_Click"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <Separator Margin="0,12"/>

                <!-- Widgets -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                    <TextBlock Text="Widgets" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    <ui:Button x:Name="ImportWidgetButton" Padding="2" Icon="Add20" Content="Import Widget" Margin="8,0,0,0" Click="ImportWidgetButton_Click"/>
                </StackPanel>
            </StackPanel>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="WidgetsList" >
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:Card x:Name="WidgetCard"  MouseDoubleClick="WidgetCard_MouseDoubleClick" Margin="0,0,0,8" Padding="10" Background="{DynamicResource UiBrushControlBackground}">
                                <ui:Card.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="WidgetActions"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="1" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="WidgetActions"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </ui:Card.Triggers>
                                <Grid>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch">
                                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Z, StringFormat=Z: {0}}" FontSize="11" Foreground="{DynamicResource UiBrushTextTertiary}"/>
                                    </StackPanel>

                                    <StackPanel x:Name="WidgetActions" Orientation="Horizontal" VerticalAlignment="Top" 
                                                Panel.ZIndex="1" Opacity="0" HorizontalAlignment="Right">
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Toggle Visibility" Click="ToggleVisibility_Click" 
                                                   Width="30" Height="30" Style="{StaticResource OpaqueSecondaryButton}" >
                                            <ui:SymbolIcon>
                                                <ui:SymbolIcon.Style>
                                                    <Style TargetType="ui:SymbolIcon">
                                                        <Setter Property="Symbol" Value="Eye20"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Visibility}" Value="False">
                                                                <Setter Property="Symbol" Value="EyeOff20"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ui:SymbolIcon.Style>
                                            </ui:SymbolIcon>
                                        </ui:Button>
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Move Up" Click="MoveUp_Click" Width="30" Height="30" Appearance="Primary">
                                            <ui:SymbolIcon Symbol="ChevronUp20"/>
                                        </ui:Button>
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Move Down" Click="MoveDown_Click" Width="30" Height="30" Appearance="Primary">
                                            <ui:SymbolIcon Symbol="ChevronDown20"/>
                                        </ui:Button>
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Edit" Click="EditWidget_Click" Width="30" Height="30" 
                                                   Style="{StaticResource OpaqueSecondaryButton}">
                                            <ui:SymbolIcon Symbol="Edit20"/>
                                        </ui:Button>
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Copy" Click="CopyWidget_Click" 
                                                   Style="{StaticResource OpaqueSecondaryButton}" 
                                                   Width="30" Height="30">
                                            <ui:SymbolIcon Symbol="Copy20"/>
                                        </ui:Button>
                                        <ui:Button Padding="2" Margin="2 0 2 0" ToolTip="Delete" Click="DeleteWidget_Click" Width="30" Height="30" Appearance="Danger">
                                            <ui:SymbolIcon Symbol="Delete20"/>
                                        </ui:Button>
                                    </StackPanel>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        
        <!-- MIDDLE - webview editor -->
        <Grid Grid.Column="1" Margin="12 40 12 12"> 
            <Border Background="{DynamicResource UiBrushSurface}" CornerRadius="8" Padding="6">
                <Grid x:Name="WebViewContainer" Background="Black">
                    <wv2:WebView2 x:Name="PreviewWebView" 
                                  xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch" 
                                  Width="{Binding _route.Width}" 
                                  Height="{Binding _route.Height}"
                                  DefaultBackgroundColor="White"/>

                    <!-- show if WebView2 is unavailable (WINE/Linux or not installed) -->
                    <StackPanel x:Name="WebViewFallbackPanel"
                                Visibility="Collapsed"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                MaxWidth="420">
                        <TextBlock Text="WebView2 is either not installed or not supported on your system."
                                   Foreground="Gray"
                                   FontSize="18"
                                   TextWrapping="Wrap"
                                   Width="400"
                                   TextAlignment="Center"
                                   Margin="0,0,0,12"/>
                        <ui:Button x:Name="OpenEditorInBrowserButton"
                                   Content="Open Editor Preview in Browser"
                                   Icon="Open24"
                                   HorizontalAlignment="Center"
                                   Click="OpenEditorInBrowser_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- RIGHT - widget editor -->
        <Border Grid.Column="2" Background="{DynamicResource UiBrushPrimary}" Padding="0 12 12 12">
            <Grid x:Name="WidgetEditorPanel">

                <StackPanel x:Name="EmptyEditorPanel">
                    <TextBlock Text="Widget Editor" FontWeight="SemiBold" Margin="0,44,0,8"/>
                    <TextBlock Text="Please select a widget to edit in the left column.&#10;When editing, hold shift to resize while preserving aspect ratio." 
                               Foreground="Gray" FontSize="13" TextWrapping="Wrap" />
                </StackPanel>

                <Grid x:Name="WidgetEditPanel" Visibility="Collapsed">
                    <DockPanel LastChildFill="True">
                        <StackPanel DockPanel.Dock="Top">
                            <TextBlock Text="Widget Editor" FontSize="18" FontWeight="Bold" Margin="0,-8,0,24"/>
                            <StackPanel Margin="0,8">
                                <TextBlock Text="Name" FontWeight="SemiBold" Margin="2 2"/>
                                <StackPanel Orientation="Horizontal">
                                    <ui:TextBox x:Name="WidgetNameBox" PlaceholderText="Widget Name..." Width="315"/>
                                    <ui:Button x:Name="DocsLinkBtn" Icon="Globe24" Width="36" Height="36" 
                                               Padding="2 0"
                                               Click="OpenWidgetDocumentation_Click"
                                               ToolTip="View Documentation"
                                               Margin="4 0 4 0"/>
                                </StackPanel>
                            </StackPanel>

                            <UniformGrid Columns="2" Rows="2" Margin="0,8">
                                <StackPanel>
                                    <TextBlock Text="Width" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetWidthBox"  Margin="4 1" PreviewTextInput="NumberOnly_PreviewTextInput" PlaceholderText="400"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Height" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetHeightBox"  Margin="4 1" PreviewTextInput="NumberOnly_PreviewTextInput" PlaceholderText="200"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="X" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetXBox" Margin="4 1" PreviewTextInput="NumberOrNegativeOnly_PreviewTextInput" PlaceholderText="50"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Y" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetYBox"  Margin="4 1" PreviewTextInput="NumberOrNegativeOnly_PreviewTextInput" PlaceholderText="70"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="ScaleX" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetScaleXBox" Margin="4 1" PreviewTextInput="NumberOrDecimalOnly_PreviewTextInput" PlaceholderText="2.5"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="ScaleY" FontWeight="SemiBold" Margin="2 2"/>
                                    <ui:TextBox x:Name="WidgetScaleYBox"  Margin="4 1" PreviewTextInput="NumberOrDecimalOnly_PreviewTextInput" PlaceholderText="2.5"/>
                                </StackPanel>
                            </UniformGrid>

                            <Separator Margin="0 32 0 8"/>
                        </StackPanel>
                        
                        <StackPanel  DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 4">
                            <Button Content="Reload Vars" Width="150" Margin="4,8,4,4" Click="ReloadVars_Click" HorizontalAlignment="Left"/>
                            <Button x:Name="SaveWidgetButton" Content="Save" Width="150" Margin="4,8,4,4"
                                    Click="SaveWidgetButton_Click" HorizontalAlignment="Right"/>
                        </StackPanel>
                        
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <!-- CSS Variables -->
                                <Expander Header="CSS Variables" IsExpanded="False" Margin="0 0 4 0">
                                    <ItemsControl x:Name="CssVarsList">
                                        <ItemsControl.Resources>
                                            <uiUtils:CssVariableTemplateSelector x:Key="CssVarSelector">
                                                <uiUtils:CssVariableTemplateSelector.DefaultTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical">
                                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                                <TextBlock VerticalAlignment="Center" Text="{Binding Name}" ToolTip="{Binding Name}" Width="180"/>
                                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" ToolTip="{Binding Description, Converter={StaticResource NullOrEmptyToNull}}" Width="150"/>
                                                            </StackPanel>
                                                            <Separator Margin="16 1"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:CssVariableTemplateSelector.DefaultTemplate>
                                                <uiUtils:CssVariableTemplateSelector.ColorTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical">
                                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                                <ui:TextBlock VerticalAlignment="Center" Text="{Binding Name}" 
                                                                           ToolTip="{Binding Name}" Width="180"/>
                                                                <views:CssColorPicker CssColor="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                                        Width="150" ToolTip="{Binding Description, Converter={StaticResource NullOrEmptyToNull}}"/>
                                                            </StackPanel>
                                                            <Separator Margin="16 1"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:CssVariableTemplateSelector.ColorTemplate
                                                ><uiUtils:CssVariableTemplateSelector.OptionsTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical">
                                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                                <ui:TextBlock VerticalAlignment="Center" Text="{Binding Name}"
                                                                           ToolTip="{Binding Name}" Width="180"/>
                                                                <ComboBox Width="150" ToolTip="{Binding Description, Converter={StaticResource NullOrEmptyToNull}}"
                                                                          ItemsSource="{Binding Type, Converter={StaticResource CssTypeOptionsConverter}}"
                                                                          SelectedItem="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                            </StackPanel>
                                                            <Separator Margin="16 1"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:CssVariableTemplateSelector.OptionsTemplate>
                                                <uiUtils:CssVariableTemplateSelector.SizeTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical">
                                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                                <ui:TextBlock VerticalAlignment="Center" Text="{Binding Name}"
                                                                           ToolTip="{Binding Name}" Width="180"/>
                                                                <ui:TextBox x:Name="SizeValueBox"
                                                                         Width="68"  ToolTip="{Binding Description, Converter={StaticResource NullOrEmptyToNull}}"
                                                                         validation:InputValidationBehavior.IsDecimalOnly="True"
                                                                         Text="{Binding Value, Converter={StaticResource CssSizeValueConverter}, Mode=OneWay}"
                                                                         Tag="{Binding}"
                                                                         Loaded="SizeValueBox_Loaded"/>
                                                                <ComboBox x:Name="SizeUnitBox"
                                                                          Width="78" ToolTip="Size Unit"
                                                                          Margin="4,0,0,0"
                                                                          ItemsSource="{Binding Type, Converter={StaticResource CssTypeOptionsConverter}}"
                                                                          SelectedItem="{Binding Value, Converter={StaticResource CssSizeUnitConverter}, Mode=OneWay}"
                                                                          Tag="{Binding}"
                                                                          Loaded="SizeUnitBox_Loaded"/>
                                                            </StackPanel>
                                                            <Separator Margin="16 1"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:CssVariableTemplateSelector.SizeTemplate>
                                            </uiUtils:CssVariableTemplateSelector>
                                        </ItemsControl.Resources>
                                        <ItemsControl.ItemTemplateSelector>
                                            <StaticResource ResourceKey="CssVarSelector"/>
                                        </ItemsControl.ItemTemplateSelector>
                                    </ItemsControl>
                                </Expander>

                                <!-- JS Variables -->
                                <Expander Header="JS Variables" IsExpanded="False" Margin="0 0 4 6">
                                    <ItemsControl x:Name="JsVarsList" ItemsSource="{Binding}">
                                        <ItemsControl.Resources>
                                            <uiUtils:JsVariableTemplateSelector x:Key="JsVarSelector">

                                                <uiUtils:JsVariableTemplateSelector.DefaultTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="150" ToolTip="{Binding Type}"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.DefaultTemplate>

                                                <uiUtils:JsVariableTemplateSelector.IntTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="150"
                                                                         Tag="{Binding}"  ToolTip="{Binding Type}"
                                                                         Loaded="JsIntBox_Loaded"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.IntTemplate>

                                                <uiUtils:JsVariableTemplateSelector.FloatTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="150"
                                                                         Tag="{Binding}" ToolTip="{Binding Type}"
                                                                         Loaded="JsFloatBox_Loaded"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.FloatTemplate>

                                                <uiUtils:JsVariableTemplateSelector.PercentTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <DockPanel >
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="170" 
                                                                              VerticalAlignment="Center" Margin="0,0,2,0"
                                                                              DockPanel.Dock="Left"/>
                                                                <TextBox x:Name="PercentBox" Width="44" Margin="2,0,0,0"
                                                                         VerticalAlignment="Center" DockPanel.Dock="Right"
                                                                         Tag="{Binding}" ToolTip="{Binding Type}"
                                                                         Loaded="JsPercentBox_Loaded"/>
                                                                <Slider x:Name="PercentSlider" Minimum="0" Maximum="100" Width="88"
                                                                        VerticalAlignment="Center" DockPanel.Dock="Right"
                                                                        IsSnapToTickEnabled="True" TickFrequency="1"
                                                                        Tag="{Binding}" ToolTip="{Binding Type}"
                                                                        Loaded="JsPercentSlider_Loaded"/>
                                                            </DockPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.PercentTemplate>

                                                <uiUtils:JsVariableTemplateSelector.BooleanTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="196" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <CheckBox Tag="{Binding}" Margin="2" Loaded="JsBoolBox_Loaded" ToolTip="{Binding Type}">
                                                                    <CheckBox.IsChecked>
                                                                        <Binding Path="Value" Converter="{StaticResource StringToBoolConverter}"/>
                                                                    </CheckBox.IsChecked>
                                                                </CheckBox>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.BooleanTemplate>

                                                <uiUtils:JsVariableTemplateSelector.EventTypeSelectTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <ComboBox Width="146" IsEditable="True" IsTextSearchEnabled="True" StaysOpenOnEdit="True" ToolTip="{Binding Type}"
                                                                          Tag="{Binding}"
                                                                          Loaded="JsEventTypeSelectBox_Loaded"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.EventTypeSelectTemplate>

                                                <uiUtils:JsVariableTemplateSelector.EventSubTypeSelectTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <ComboBox Width="146" IsEditable="True" IsTextSearchEnabled="True" StaysOpenOnEdit="True" ToolTip="{Binding Type}"
                                                                          Tag="{Binding}"
                                                                          Loaded="JsEventSubTypeSelectBox_Loaded"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.EventSubTypeSelectTemplate>

                                                <uiUtils:JsVariableTemplateSelector.StringSelectTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                                <ComboBox Width="146" Tag="{Binding}" Loaded="JsStringSelectBox_Loaded" ToolTip="{Binding Type}"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.StringSelectTemplate>

                                                <uiUtils:JsVariableTemplateSelector.FileVarTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <StackPanel Orientation="Horizontal">
                                                                <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" VerticalAlignment="Top" Margin="0,0,8,0"/>
                                                                <StackPanel Orientation="Vertical" Tag="{Binding}" Loaded="JsFileVar_Loaded" ToolTip="{Binding Type}"/>
                                                            </StackPanel>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.FileVarTemplate>

                                                <uiUtils:JsVariableTemplateSelector.EventTypeListTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172" 
                                                                          HorizontalAlignment="Left"
                                                                          VerticalAlignment="Top" Margin="0,0,8,4"/>
                                                            <Expander BorderBrush="Gray" BorderThickness="1" Padding="4" 
                                                                      Width="288" HorizontalAlignment="Center" ToolTip="{Binding Type}"
                                                                      Margin="0,0,0,2" IsExpanded="False" Header="Select EventTypes"
                                                                      Tag="{Binding}" Loaded="JsEventTypeList_Loaded"/>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.EventTypeListTemplate>

                                                <uiUtils:JsVariableTemplateSelector.EventSubTypeListTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical" Margin="0,4,0,4">
                                                            <ui:TextBlock Text="{Binding Name}" ToolTip="{Binding Name}" Width="172"
                                                                          VerticalAlignment="Top" Margin="0,0,8,4" HorizontalAlignment="Left"/>
                                                            <Expander BorderBrush="Gray" BorderThickness="1" Padding="4"
                                                                      Width="288" HorizontalAlignment="Center" ToolTip="{Binding Type}"
                                                                      Margin="0,0,0,2" IsExpanded="False" Header="Select Event Groups"
                                                                      Tag="{Binding}" Loaded="JsEventSubTypeList_Loaded"/>
                                                            <Separator Margin="16,2"/>
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </uiUtils:JsVariableTemplateSelector.EventSubTypeListTemplate>

                                            </uiUtils:JsVariableTemplateSelector>
                                        </ItemsControl.Resources>
                                        <ItemsControl.ItemTemplateSelector>
                                            <StaticResource ResourceKey="JsVarSelector"/>
                                        </ItemsControl.ItemTemplateSelector>
                                    </ItemsControl>
                                </Expander>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Grid>
            </Grid>
        </Border>

    </Grid>
</ui:FluentWindow>
